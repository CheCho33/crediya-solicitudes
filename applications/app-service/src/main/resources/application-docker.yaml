server:
  port: 8080
spring:
  application:
    name: "solicitudes"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

postgresql:
  host: "dbCrediya"
  port: 5432
  database: "dbCrediYa"
  schema: "public"
  username: "postgres"
  password: "root"

adapters:
  r2dbc:
    host: "dbCrediya"
    port: 5432
    database: "dbCrediYa"
    schema: "public"
    username: "postgres"
    password: "root"

jwt:
  secret: "CrediYaSecretKey2024ForJWTTokenGenerationAndValidation"
  expiration: 86400000
